# pip3 install -v -e . 运行失败
pip install -v -e . --no-build-isolation，需要加上--no-build-isolation，好像是pip在安装包时，会构建一个隔离环境，导致找不到torch
# 训练时提示找不到gpu
代码里检查gpu显存的时候，使用的是nvidia-smi，在wsl下面好像不行，所以让ai改了一下
# 只训练COCO的猫狗类别时，发现偶尔会把熊、羊、长颈鹿等误识别成猫和狗
可以考虑之后加入一些容易混淆的类别进行训练，也许能够区分开类别间的差异，减少这些类别被误判成猫狗的概率